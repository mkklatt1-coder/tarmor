[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn tarmor_config.wsgi:application --bind 0.0.0.0:$PORT"
predeploy = "python manage.py migrate_schemas && python -m tarmor_config.create_admins"
restartPolicyType = "ON_FAILURE"